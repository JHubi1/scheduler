name: scheduler
description: A modern, class-based task scheduling system, primarily for a Dart server environment.
publish_to: none

version: 0.1.0
repository: https://github.com/JHubi1/scheduler

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.19.0
  collection: ^1.19.1
  retry: ^3.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
